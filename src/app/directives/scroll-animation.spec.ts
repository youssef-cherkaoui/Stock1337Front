import { ScrollAnimation } from './scroll-animation';

describe('ScrollAnimation', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnimation();
    expect(directive).toBeTruthy();
  });
});
